## windows 部署 front

在服务器上执行以下命令

```powerShall
./scripts/deploy-front.ps1 -cosSecretId "yourCosSecretId" -cosSecretKey "yourCosSecretKey" -cosBucket "yourCosBucket" -region "yourRegion" -cdnUrl "yourCdnUrl"
```
